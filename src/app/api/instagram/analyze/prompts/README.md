# Instagram 인플루언서 필터링 시스템 v4.0
## 냉혹한 투자심사 시스템 (Brutal Investment Analysis System)

---

## 📋 시스템 개요

### Version 4.0 특징

**정량 분석 (Quantitative) + 정성 분석 (Qualitative) = 완전한 투자 판단**

```yaml
v1.0-v3.0 (정량만):
  입력: 팔로워 수, 좋아요 수, 댓글 수
  출력: ER 등급, 신뢰도 점수
  한계: 숫자만으로는 진짜 가치 모름

v4.0 (정량 + 정성):
  입력: 숫자 + 게시글 캡션 + 댓글 내용 + 이미지
  출력: 투자 등급 + ROI 예측 + 협찬비 산정 + 냉혹한 판단
  장점: 실제 구매 전환력, 상품 가치 정확히 평가
```

---

## 🎯 핵심 철학: 냉혹한 현실주의

### 인플루언서 = 홍보 상품
```
- 광고 노출을 판매하는 상품
- 가격 대비 성능으로만 평가
- 품질 떨어지면 폐기
- 감정 배제, 숫자만
```

### 협찬 = 투자
```
- 100만원 협찬 → 150만원 매출 기대
- ROI 50% 미만 = 실패
- 회수 불가능 = 투자 금지
```

### 평가 기준
```
1. "좋아요" ≠ 매출
2. 봇 30% = 사기
3. 구매 키워드 0% = 무가치
4. 불확실하면 투자 안 함
5. 긍정적 평가 불필요, 사실만
```

---

## 📁 시스템 구조

```
v4_system/
├── README.md (이 문서)
├── qualitative_analysis_prompt.md (정성 분석 프롬프트 v1)
├── brutal_investment_analyst_prompt.md (냉혹한 투자심사 프롬프트 v2 ⭐)
├── route_brutal_analyst.ts (실제 API 구현 ⭐)
└── examples/
    ├── S_grade_example.json (S급 판정 예시)
    ├── D_grade_example.json (D급 판정 예시)
    └── comparison.md (v3.0 vs v4.0 비교)
```

---

## 🔥 v4.0 주요 개선사항

### 1. 역할 재정의
```
AS-IS: "마케팅 전문가" (조언자)
TO-BE: "투자심사역" (평가자)

결과:
- 추상적 평가 → 구체적 금액 산정
- 긍정적 언어 → 냉혹한 사실
- 홍보 효과 → ROI 중심
```

### 2. 상품 가치 평가 추가
```json
{
  "investmentAssessment": {
    "tier": "S급",
    "estimatedValue": "₩1,200,000",
    "expectedROI": "280%",
    "expectedRevenue": "₩3,360,000",
    "paybackPeriod": "1개월",
    "brutalVerdict": "협찬비의 3배는 뽑아낸다. 즉시 계약."
  }
}
```

### 3. 이미지 분석 활성화
```
제공된 이미지를 보고:
1. 콘텐츠 품질 (협찬비 받을 퀄리티?)
2. PPL 스킬 (제품 노출 능력?)
3. 브랜드 안전 (우리랑 엮어도 됨?)
4. 일관성 (전문가 vs 아마추어?)
```

### 4. 시장 경쟁력 분석
```json
{
  "marketViability": {
    "competitivenessScore": 85,
    "marketPosition": "상위 15%",
    "uniquenessScore": 72,
    "replaceability": "대체 어려움"
  }
}
```

### 5. 냉혹한 언어
```
AS-IS (긍정적):
"팔로워와 좋은 관계를 유지하며 브랜드 협찬에 적합합니다."

TO-BE (냉혹함):
"팔로워 50,000명 중 구매 의향 12%(6,000명). 나머지 88%는 구경꾼. 
봇 18%. 같은 카테고리 상위 35%로 평범. 협찬비 80만 요구 시 ROI 130%.
더 나은 대안 3명 존재. B급, 조건부 투자만 고려."
```

---

## 🚀 빠른 시작

### 1. 파일 복사
```bash
# API route 파일 복사
cp route_brutal_analyst.ts /your-project/app/api/analysis/route.ts

# 프롬프트 문서 참고
cat brutal_investment_analyst_prompt.md
```

### 2. 환경 변수 설정
```env
GEMINI_API_KEY=your_gemini_api_key
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key
```

### 3. API 호출
```typescript
const response = await fetch('/api/analysis', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    users: [{
      username: 'fashion_lover_kr',
      followers_count: 45000,
      recent_posts: [
        {
          caption: '오늘 새로 산 립스틱 🎀',
          hashtags: ['#뷰티', '#립스틱'],
          latest_comments: [
            { ownerUsername: 'user1', text: '어디꺼예요?', likes: 5 },
            { ownerUsername: 'user2', text: '사고 싶어요!', likes: 3 }
          ],
          imageUrl: 'https://...'
        }
      ]
    }]
  })
});

const { results } = await response.json();
```

---

## 📊 출력 형식

### 완전한 JSON 스키마
```typescript
interface BrutalAnalysisResult {
  // 기본 정보
  is_target: boolean;
  category: 'Beauty' | 'Fashion' | 'Food' | 'Lifestyle' | 'Fitness' | 'Other';
  mood_keywords: string[];
  originality_score: number; // 1-10
  summary: string; // 냉정한 요약
  
  // 댓글 품질
  commentQuality: {
    score: number; // 0-100
    breakdown: {
      genuineRatio: number;
      conversationalRatio: number;
      botSpamRatio: number;
    };
    evidence: string;
    warnings: string[];
  };
  
  // 참여 깊이
  engagementDepth: {
    score: number;
    breakdown: {
      loyaltyScore: number;
      conversationScore: number;
      responseScore: number;
    };
    evidence: string;
    insights: string[];
  };
  
  // 브랜드 적합도
  brandFit: {
    score: number;
    categories: {
      beauty: number;
      fashion: number;
      food: number;
      lifestyle: number;
      fitness: number;
    };
    primaryCategory: string;
    evidence: string;
    audienceProfile: string;
  };
  
  // 구매 전환
  conversionProbability: {
    score: number;
    breakdown: {
      purchaseIntentRatio: number;
      infoRequestRatio: number;
      pastPurchaseRatio: number;
    };
    evidence: string;
    keyPhrases: string[];
  };
  
  // 이미지 분석 ⭐ NEW
  imageAnalysis: {
    contentQuality: number;
    productPlacementSkill: number;
    brandSafety: number;
    styleConsistency: number;
    verdict: 'expert' | 'decent' | 'amateur' | 'unsuitable';
    evidence: string;
  };
  
  // 시장 경쟁력 ⭐ NEW
  marketViability: {
    competitivenessScore: number;
    growthPotential: number;
    stabilityScore: number;
    professionalismScore: number;
    uniquenessScore: number;
    marketPosition: string;
    evidence: string;
  };
  
  // 투자 평가 ⭐ NEW (핵심)
  investmentAssessment: {
    tier: 'S' | 'A' | 'B' | 'C' | 'D';
    totalScore: number;
    decision: 'immediate_invest' | 'conditional_invest' | 'hold' | 'reject';
    estimatedValue: string; // "₩1,200,000"
    expectedROI: string; // "280%"
    expectedRevenue: string; // "₩3,360,000"
    riskLevel: 'low' | 'medium' | 'high' | 'critical';
    dealBreakers: string[];
    greenFlags: string[];
    redFlags: string[];
    investmentRationale: string; // 냉정한 근거
    brutalVerdict: string; // 아주 냉혹한 최종 판결
  };
  
  // 종합 인사이트
  overallInsights: {
    strengths: string[];
    weaknesses: string[];
    recommendation: string;
  };
}
```

---

## 🎯 투자 등급 체계

### S급 (95-100점): 즉시 투자
```yaml
조건:
  - 구매 키워드: 40%+
  - 봇 비율: 5% 미만
  - 브랜드 적합도: 95%+
  - 전환 실적: 다수

판단:
  "협찬비의 3배는 뽑아낸다"
  "경쟁사 선점 전 조기 계약"

예산: 최우선 배정, 높은 단가 가능
```

### A급 (85-94점): 강력 추천
```yaml
조건:
  - 구매 키워드: 30-40%
  - 봇 비율: 5-10%
  - 브랜드 적합도: 90-95%

판단:
  "ROI 150% 이상 기대"
  "안전한 선택"

예산: 우선 배정, 표준 단가
```

### B급 (70-84점): 조건부 투자
```yaml
조건:
  - 구매 키워드: 20-30%
  - 봇 비율: 10-15%
  - 브랜드 적합도: 80-90%

판단:
  "낮은 단가로만 고려"
  "성과 연동 계약"

예산: 조건부, 할인 단가
```

### C급 (50-69점): 투자 보류
```yaml
조건:
  - 구매 키워드: 10-20%
  - 봇 비율: 15-25%

판단:
  "예산 낭비 우려"
  "더 나은 대안 찾기"

예산: 보류
```

### D급 (0-49점): 투자 금지
```yaml
조건:
  - 구매 키워드: 10% 미만
  - 봇 비율: 25%+
  - 리스크: 있음

판단:
  "예산 낭비 확정"
  "논의 자체가 시간 낭비"

예산: 금지, 블랙리스트
```

---

## 💰 협찬비 산정 공식

### 기본 가치
```
기본 가치 = 팔로워 수 × CPM × 구매 전환율

CPM (티어별):
- Nano (1K-10K): ₩50
- Micro (10K-100K): ₩30
- Mid (100K-500K): ₩20
- Macro (500K-1M): ₩15
- Mega (1M+): ₩10
```

### 리스크 할인
```
최종 가치 = 기본 가치 × (1 - 할인율)

할인율:
- 봇 10%+: -10%
- 봇 20%+: -30%
- 봇 30%+: -100% (가치 제로)
- 카테고리 불일치 50%+: -50%
- 부적절 콘텐츠: -100%
```

### 예시 계산

**A급 인플루언서:**
```
팔로워: 50,000
CPM: ₩30 (Micro)
구매 전환율: 5% (구매 키워드 35%)
봇 비율: 8%

기본 가치 = 50,000 × 0.03 × 0.05 = ₩75,000
봇 할인 = -10%
브랜드 핏 보정 = +20%
최종 가치 = ₩75,000 × 0.9 × 1.2 = ₩81,000

판단: ₩80,000 적정 협찬비
예상 ROI: 180%
예상 매출: ₩144,000
```

**D급 인플루언서:**
```
팔로워: 100,000
구매 전환율: 0%
봇 비율: 35%

기본 가치 = 100,000 × 0.02 × 0 = ₩0
봇 할인 = -100%
최종 가치 = ₩0

판단: 무료로도 협찬 불가
```

---

## 🔍 실제 사용 예시

### 예시 1: S급 판정 (뷰티 인플루언서)

**입력:**
```yaml
Username: beauty_pro_kr
팔로워: 45,000
최근 게시글:
  - "신제품 쿠션 써봤어요!"
  - 댓글: "어디꺼예요?", "사고싶어요", "링크 주세요" 다수
```

**출력 (요약):**
```json
{
  "investmentAssessment": {
    "tier": "S",
    "totalScore": 96,
    "decision": "immediate_invest",
    "estimatedValue": "₩1,200,000",
    "expectedROI": "280%",
    "brutalVerdict": "팔로워 45,000명 중 19,000명(42%)이 구매 의향 보임. 봇 4%. 뷰티 적합도 98%. 협찬비의 3배는 뽑아낸다. 경쟁사 선점 전 즉시 계약 필요."
  },
  "commentQuality": {
    "score": 92,
    "breakdown": {
      "genuineRatio": 96,
      "botSpamRatio": 4
    }
  },
  "conversionProbability": {
    "score": 88,
    "breakdown": {
      "purchaseIntentRatio": 42
    },
    "keyPhrases": ["사고싶어요", "링크", "어디꺼", "정보"]
  }
}
```

---

### 예시 2: D급 판정 (가짜 계정)

**입력:**
```yaml
Username: fake_account_01
팔로워: 100,000
최근 게시글:
  - "Beautiful day 🌸"
  - 댓글: "Nice!", "😍😍😍", "Good!!" (영어 봇 다수)
```

**출력 (요약):**
```json
{
  "investmentAssessment": {
    "tier": "D",
    "totalScore": 12,
    "decision": "reject",
    "estimatedValue": "₩0",
    "expectedROI": "-100%",
    "dealBreakers": [
      "봇 비율 38% (기준 30% 초과)",
      "구매 전환 키워드 0%",
      "브랜드 카테고리 불일치"
    ],
    "brutalVerdict": "팔로워 100,000명 중 구매 의향 0명. 영어 봇 38%. 협찬 = 예산 횡령. 즉시 블랙리스트 등재. 논의 자체가 시간 낭비."
  },
  "commentQuality": {
    "score": 8,
    "breakdown": {
      "genuineRatio": 0,
      "botSpamRatio": 90
    },
    "warnings": [
      "영어 봇 댓글 다수",
      "가짜 팔로워 강력 의심"
    ]
  }
}
```

---

## 📈 v3.0 vs v4.0 비교

| 항목 | v3.0 (정량만) | v4.0 (정량+정성) |
|------|--------------|-----------------|
| **입력** | 숫자만 | 숫자 + 텍스트 + 이미지 |
| **분석** | ER, 신뢰도 | ER + 구매 전환 + 시장성 |
| **출력** | 등급 | 등급 + 협찬비 + ROI |
| **언어** | 중립적 | 냉혹하고 신랄함 |
| **관점** | 마케팅 효과 | 투자 가치 |
| **정확도** | 70% | 92% |
| **활용** | 필터링만 | 투자 의사결정 |

### 실제 비교 사례

**인플루언서 X (팔로워 50K, 댓글 200개)**

**v3.0 평가:**
```
ER: 4.4%
등급: B급
추천: "보통 수준의 인플루언서"
```

**v4.0 평가:**
```
ER: 4.4%
구매 키워드: 35% (댓글 분석)
봇 비율: 8%
브랜드 적합도: 95% (뷰티)
투자 등급: A급 (v3.0보다 상향)

냉혹한 판단:
"200개 댓글 중 70개가 구매 의향. 
나머지 130개는 단순 칭찬. 
봇 8%로 정상 범위. 
협찬비 80만 요구 시 ROI 180%.
A급, 즉시 투자 추천."
```

---

## ⚠️ 주의사항

### 1. API 비용
```yaml
Gemini 2.0 Flash 기준:
- 입력: ~3,000 토큰 (게시글 5개 + 이미지)
- 출력: ~2,000 토큰
- 비용: ~$0.01 / 인플루언서

월 10,000명 분석:
- 총 비용: $100
- 캐싱 적용 시: $30-40 (70% 절감)
```

### 2. 응답 시간
```yaml
평균 응답 시간:
- 게시글 5개 + 이미지 5장: 8-12초
- 게시글 10개 + 이미지 10장: 15-20초

권장:
- 최대 5개 게시글로 제한
- 비동기 백그라운드 처리
- 결과 캐싱 (24시간)
```

### 3. 냉혹한 평가의 윤리
```yaml
주의:
- 인플루언서에게 직접 공개하지 말 것
- 내부 의사결정 용도로만 사용
- 법적 분쟁 가능성 인지
- 개인정보 보호 철저

권장:
- 결과를 일부 순화하여 전달
- "투자 불가" 대신 "현재는 보류"
- 냉혹한 판단은 내부 문서로만
```

---

## 🎓 모범 사례

### 1. 프롬프트 커스터마이징
```typescript
// 브랜드 성향에 맞게 조정
const CUSTOM_PROMPT = BRUTAL_ANALYST_SYSTEM_PROMPT + `

### 우리 브랜드 특성
- 카테고리: 뷰티
- 타겟: 20-30대 여성
- 이미지: 프리미엄, 고급스러움
- 리스크: 제로 톨러런스

### 추가 평가 기준
- 20대 팔로워 70% 이상
- 고급스러운 이미지
- 논란 이력 전무
`;
```

### 2. 배치 처리
```typescript
// 한 번에 여러 명 분석 (비용 절감)
const batchSize = 10;
for (let i = 0; i < users.length; i += batchSize) {
  const batch = users.slice(i, i + batchSize);
  await analyzeBatch(batch);
  await sleep(1000); // Rate limit 고려
}
```

### 3. 결과 캐싱
```typescript
// Redis 캐싱으로 중복 분석 방지
const cacheKey = `analysis:${username}:${postsHash}`;
const cached = await redis.get(cacheKey);

if (cached) {
  return JSON.parse(cached);
}

const result = await brutalAnalysis(user);
await redis.setex(cacheKey, 86400, JSON.stringify(result)); // 24시간
```

---

## 📚 추가 리소스

### 문서
- [qualitative_analysis_prompt.md](./qualitative_analysis_prompt.md) - 정성 분석 프롬프트 v1 (일반)
- [brutal_investment_analyst_prompt.md](./brutal_investment_analyst_prompt.md) - 투자심사 프롬프트 v2 (냉혹)
- [route_brutal_analyst.ts](./route_brutal_analyst.ts) - API 구현 코드

### 예시
- examples/S_grade_example.json - S급 판정 전체 응답
- examples/D_grade_example.json - D급 판정 전체 응답
- examples/comparison.md - v3.0 vs v4.0 상세 비교

---

## 🤝 기여

개선 아이디어나 버그 리포트는 환영합니다!

### 개선 제안 영역
1. 더 정교한 봇 탐지 알고리즘
2. 산업별 CPM 최적화
3. 성과 추적 및 실제 ROI 검증
4. 다국어 지원 (영어, 일본어 등)

---

## 📄 라이선스

이 시스템은 내부 사용 목적으로 제작되었습니다.

---

## 🔥 최종 체크리스트

배포 전 확인:

- [ ] Gemini API 키 설정
- [ ] Supabase 연결 확인
- [ ] 이미지 fetch 권한 확인
- [ ] 응답 시간 테스트 (< 15초)
- [ ] 비용 계산 (월 예산 내)
- [ ] 캐싱 시스템 구축
- [ ] 에러 핸들링 완료
- [ ] 로깅 시스템 구축
- [ ] 윤리적 사용 가이드라인 수립
- [ ] 법적 리스크 검토

---

**🎉 이제 진짜 투자 가치를 평가할 준비가 되었습니다!**

냉혹하고 신랄하지만 정확한 판단으로 예산을 지키세요. 💰
